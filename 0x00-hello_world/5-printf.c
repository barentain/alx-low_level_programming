#include <stdio.h>

/**
 * main - prints a statement  followed by a new line
 *
 * Return: ends the program with a 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
